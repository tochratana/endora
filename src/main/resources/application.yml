spring:
  # Default profile configuration
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}

  # PostgreSQL Configuration (for User entity)
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/endora}
    username: ${DB_USERNAME:oudom}
    password: ${DB_PASSWORD:123}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  # MongoDB Configuration (for dynamic tables)
  data:
    mongodb:
      host: ${MONGODB_HOST:localhost}
      port: ${MONGODB_PORT:27017}
      database: ${MONGODB_DATABASE:endora}
      # username: your_mongo_username  # uncomment if auth enabled
      # password: your_mongo_password  # uncomment if auth enabled

  # Email Configuration for OTP
  mail:
    host: smtp.gmail.com
    port: 587
    username: endora.istad@gmail.com  # Replace with your Gmail
    password: utnu dtwk xyre ebkh   # Replace with your Gmail app password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 3000
          writetimeout: 5000

  # Keycloak Configuration - FIXED: Match your actual Keycloak server
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://endora-oauth2.istad.co/realms/endora_api

# Keycloak Admin Client Configuration
keycloak:
  server-url: ${KEYCLOAK_SERVER_URL:https://endora-oauth2.istad.co/}
  realm: ${KEYCLOAK_REALM:endora_api}
  client-id: ${KEYCLOAK_CLIENT_ID:spring-admin}
  client-secret: ${KEYCLOAK_CLIENT_SECRET:uliUJohOY9OI1jM1SnFrog5iJ23JXWJN}
  admin:
    client-id: ${KEYCLOAK_ADMIN_CLIENT_ID:admin-cli}
    client-secret: ${KEYCLOAK_ADMIN_CLIENT_SECRET:b2L6hrMg76JLk4GSLd8CGMPRJNnAOko2}

# Server Configuration
server:
  port: ${PORT:8080}
