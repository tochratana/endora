spring:
  # PostgreSQL Configuration (for User entity)
  datasource:
    url: jdbc:postgresql://localhost:5432/pgsql
    username: postgres
    password: 1605
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  # MongoDB Configuration (for dynamic tables)
  data:
    mongodb:
      uri: mongodb://localhost:27017/endora
      host: localhost
      port: 27017
      database: endora
      # username: your_mongo_username  # uncomment if auth enabled
      # password: your_mongo_password  # uncomment if auth enabled

  # Email Configuration for OTP
  mail:
    host: smtp.gmail.com
    port: 587
    username: endora.istad@gmail.com  # Replace with your Gmail
    password: utnu dtwk xyre ebkh   # Replace with your Gmail app password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connection timeout: 5000
          timeout: 3000
          write timeout: 5000

  # Keycloak Configuration - FIXED: Match your actual Keycloak server
  security:
    oauth2:
      resource server:
        jwt:
          issuer-uri: https://endora-oauth2.istad.co/realms/endora_api

  # Virtual Threads
  threads:
    virtual:
      enabled: true

  # Add filter registration to ensure proper order
  main:
    allow-bean-definition-overriding: true

keycloak:
  server-url: https://endora-oauth2.istad.co/
  realm: endora_api
  # FIXED: Use the correct client ID for login (not admin-cli)
  client-id: spring-admin  # This should be your actual client for authentication
  client-secret: uliUJohOY9OI1jM1SnFrog5iJ23JXWJN  # Get this from Keycloak
  admin:
    client-id: admin-cli
    client-secret: b2L6hrMg76JLk4GSLd8CGMPRJNnAOko2

# OTP Configuration
app:
  otp:
    expiry-minutes: 5
    max-attempts: 3
  jwt:
    base-secret: your-super-secret-key-change-this-in-production-make-it-long-and-random
    expiration: 604800000 # 7 days in milliseconds


server:
  port: 8080

# Make MongoDB connection optional
management:
  health:
    mongo:
      enabled: false

# Logging
logging:
  level:
    com.tochratana.api: DEBUG
    org.keycloak: DEBUG
    rinsanom.com.springtwodatasoure.security: DEBUG # Add this for JWT debugging
    io.jsonwebtoken: DEBUG # Add this for JWT debugging